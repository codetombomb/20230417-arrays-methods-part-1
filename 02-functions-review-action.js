// function declaration
const gumBallMachine = (money) => {
    // If money is exaclty .25
    if(money === 0.25){
      console.log("give user a gumball")
    } else {
      console.log("insert quarter")
    }
    
    /* 
    What needs to happen in 
    order for our function to give
    a user a gumball?
    */
  }
  
  // function call
  gumBallMachine([])